classdef StateButtonController < handle
    % Controls the state of several StateButtons which do not(!) need to be
    % grouped in a button group (due to the bad positioning possibilities).
    % 
    
    
end